# Task: Scaffold Transcriber Package

**Session ID:** orch-20260215-transcriber  
**Priority:** P0  
**Dependencies:** None  
**Assigned To:** /mode-code

---

## 📋 Objective

Create the `tools/transcriber/` package with all scaffolding files so subsequent tasks can implement the modules.

## 🎯 Scope

**In Scope:**
- `tools/transcriber/package.json` with all dependencies
- `tools/transcriber/tsconfig.json`
- `tools/transcriber/.env.example`
- Empty stub files for `src/index.ts`, `src/audio.ts`, `src/groq.ts`, `src/utils.ts`
- Add `transcribe` script to root `package.json`

**Out of Scope:**
- Implementing any module logic (that's Tasks 2-4)

## 📚 Context

This is a pnpm + Turborepo monorepo. The `pnpm-workspace.yaml` already includes `tools/*`.

### Package Configuration

```json
{
  "name": "@jstar/transcriber",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "transcribe": "tsx src/index.ts"
  },
  "dependencies": {
    "groq-sdk": "^0.8.0",
    "commander": "^12.0.0",
    "cli-progress": "^3.12.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "@types/cli-progress": "^3.11.0",
    "@types/node": "^20.0.0"
  }
}
```

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "esModuleInterop": true,
    "strict": true,
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

### .env.example

```
GROQ_API_KEY=your_groq_api_key_here
```

### Stub Files

Each `src/*.ts` file should export a comment placeholder:

```typescript
// src/audio.ts
// Audio extraction and chunking using ffmpeg
// Implemented in Task 02

export {};
```

### Root package.json Addition

Add to `"scripts"`:

```json
"transcribe": "pnpm --filter @jstar/transcriber transcribe --"
```

## ✅ Definition of Done

- [ ] `tools/transcriber/package.json` exists with correct deps
- [ ] `tools/transcriber/tsconfig.json` exists
- [ ] `tools/transcriber/.env.example` exists
- [ ] All 4 stub source files exist in `src/`
- [ ] `pnpm install` succeeds from repo root
- [ ] `npx tsc --noEmit` passes in `tools/transcriber/`
- [ ] Root `package.json` has `transcribe` script

## 📁 Expected Artifacts

| File | Purpose |
|------|---------|
| `tools/transcriber/package.json` | Package config + deps |
| `tools/transcriber/tsconfig.json` | TypeScript config |
| `tools/transcriber/.env.example` | API key template |
| `tools/transcriber/src/index.ts` | CLI entry stub |
| `tools/transcriber/src/audio.ts` | Audio module stub |
| `tools/transcriber/src/groq.ts` | Groq module stub |
| `tools/transcriber/src/utils.ts` | Utilities stub |

---

*Generated by /mode-orchestrator — Session: orch-20260215-transcriber*
