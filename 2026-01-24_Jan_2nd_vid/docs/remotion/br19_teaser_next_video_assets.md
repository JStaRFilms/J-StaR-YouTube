# 🎨 Asset Manifest: BR-19 Teaser Next Video

## 1. Code-Generatable Components ✅
*These can be built entirely with React/SVG code*

| Component | Description | Complexity |
|-----------|-------------|------------|
| `MockupScene.tsx` | Opening scene with pulsing mockup and text. | Medium |
| `QuestionScene.tsx` | Typewriter text and bouncing question mark. | Low |
| `TransformationScene.tsx` | Complex scene with drawing arrow and particle flow. | High |
| `CtaScene.tsx` | Final call to action with pulse. | Low |
| `DeviceFrame.tsx` | Reusable window/phone container styling. | Low |
| `AnimatedArrow.tsx` | SVG path animation with gradient. | Medium |
| `CodeParticles.tsx` | Floating syntax symbols animation. | Medium |
| **`VoidBackground.tsx`** | Programmatic dark background with radial gradients/noise. | Medium |
| **`MockDashboard.tsx`** | CSS/Flexbox constructed "fake" dashboard UI (charts, sidebar). | High |
| **`MockVsCode.tsx`** | React component mimicking VS Code window with syntax highlighting. | High |

---

## 2. Image Prompts for User 🖼️
*REMOVED: We are building everything with code per your request.*

---

## 3. External Assets Needed 📦
*These require you to provide - stock footage, your logos, audio files*

| Asset | Type | Description | Action |
|-------|------|-------------|--------|
| Font Files | TTF/WOFF | Inter or Roboto (Google Fonts) | I will load via `@remotion/google-fonts` |

---

## 4. Your Choices ⚡

**We are going fully programmatic.** 🚀

No action needed from you for assets! I will build:
1. `VoidBackground`: Dark CSS gradients + noise
2. `MockDashboard`: Pure CSS shapes to look like a high-end UI
3. `MockVsCode`: Pure CSS window with text contents

