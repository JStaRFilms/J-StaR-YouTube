# 🎨 Asset Manifest: Short_2_Empty_Setup_

## 1. Code-Generatable Components ✅
*These can be built entirely with React/SVG code*

| Component | Description | Complexity |
|-----------|-------------|------------|
| `GridBackground.tsx` | A light mode, subtle #E5E7EB grid on a #FAFAFA background | Low |
| `AiIcon.tsx` | A stylized robot/sparkle AI icon created with SVG paths, clean and geometric | Medium |
| `EmptyFolderOutline.tsx` | A dashed-stroke SVG folder that can be animated with `strokeDashoffset` | Low |
| `UiCard.tsx` | High-end, generic UI cards (Context, Standards, Design, Checks) with soft shadows | Medium |
| `MissingStamp.tsx` | A red "MISSING" or "X" stamp using typography and pure CSS shapes | Low |
| `LoadingSpinner.tsx` | A large circular spinner that transforms into a 3D question mark | High |
| `ComponentPile.tsx` | Assorted UI elements (buttons, inputs) that fly together and then collapse | High |
| `SystemScaffold.tsx` | An animated blueprint skeleton layout (cyan) | High |

---

## 2. Image Prompts for User 🖼️
*I CANNOT generate these myself. I am providing prompts for YOU to generate.*

> **Instructions for User:**
> Use these prompts with your preferred image generation tool (Midjourney, DALL-E, generate_image tool, etc.)
> After generating, save the images to `public/assets/short2-empty-setup-/`

*(Note: For this , we aim to build nearly everything in code for maximum crispness and animation fidelity in light mode. Thus, zero AI-generated images are strictly required. However, if you'd like custom illustrations instead of SVG icons, here are optional prompts:)*

| Asset | Prompt | Size | Save As |
|-------|--------|------|---------|
| (Optional) 3D Robot Head | "A clean, glossy white 3d robot head with a subtle cyan glowing eye, light gray background, minimalist, highly detailed, isometric style" | 1024x1024 | `ai-head.png` |
| (Optional) Red Error Badge | "A sleek 3D glossy red X badge, light mode, studio lighting, highly realistic" | 512x512 | `error-badge.png` |

**Total prompts**: 2 (Optional) images for you to generate

---

## 3. External Assets Needed 📦
*These require you to provide - stock footage, your logos, audio files*

| Asset | Type | Description | Action |
|-------|------|-------------|--------|
| Voiceover Track | MP3/WAV | Voiceover for "I used to think the AI was the problem..." | You provide |

---

## 4. Your Choices ⚡

Tell me for each:

| Item | Options |
|------|---------|
| Visual style | [x] I want 100% Code-Generatable SVG components / [ ] I will provide the 3D images |
| Voiceover | [x] I'll provide it later (we build with dummy timing) / [ ] I have the file now |

---

## After You Decide

**If you want me to proceed with code components:**
Reply: "Build the components and use dummy timing for audio"

**If you want to generate images first:**
Run the prompts yourself, then reply: "Assets ready in public/assets/short2-empty-setup-/"

**If you're providing your own assets:**
Reply: "I'm providing: [list files] - they're in public/assets/short2-empty-setup-/"
